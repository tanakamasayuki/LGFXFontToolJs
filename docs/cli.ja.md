# lgfx-font — CLI 仕様（草案）

- 状態: **草案。詰めながら書き換える**
- 対象読者: この CLI を実装する人・使う人
- 最終更新: 2026-08-28

## 1. 位置づけ

**Web UI（Viewer / Generator / Converter / Inspector）と役割を分ける。**

Web UI は書体を探し、文字集合を組み立て、目で見て決めるための道具。CLI が引き受けるのは
**ブラウザに原理的にできないこと**だけにする。

| | Web UI | CLI |
| --- | --- | --- |
| 書体を探す・選ぶ | ○ | × |
| 文字集合を組み立てて眺める | ○ | × |
| 一度だけ変換して落とす | ○ | △ |
| **同じ操作を毎ビルド繰り返す** | × | **○** |
| **同じ結果を CI で再現する** | × | **○** |

### やらないこと

- **ソースコードの走査**（どの文字を使っているかの自動収集）— プロジェクト固有なので利用者側
- **git 操作** — CI の仕事
- **書体選び・文字集合の組み立て UI** — Web UI の仕事
- **凝った画像出力** — 画像は生成物ではなく**生成物の確認**（§8）

## 2. 設計の指針

1. **引数だけで完結する。** 設定ファイルを書かないと何も出ない道具にしない。
   単発利用が起点で、継続利用は「引数を保存したもの」にすぎない
2. **簡潔さを優先する。** ネットワークとローカルの資源は多少使ってよい。
   数 MB の取得やキャッシュより、**コマンドと概念の少なさ**を優先する
3. **出力結果が正。** 同じ環境で同じ入力なら同じバイト列が出る

### 継続利用は「同じコマンドの別モード」

| 使い方 | 形 |
| --- | --- |
| 単発 | `lgfx-font build <引数>` |
| 日常の増減 | `npm run font`（= 保存した `build`） |
| CI 検証 | `npm run font -- --check` |

**設定ファイルは作らない。** 複数ターゲットは npm script の `&&` で足りる。
必要になれば「保存された引数」として後から互換に足せる。

## 3. コマンド

```
lgfx-font build    <入力> <文字> <出力>     フォントデータを作る
lgfx-font inspect  <file>                    既存フォントを調べる
lgfx-font charset  <file> --normalize        文字集合ファイルを正準形にする
```

3 つ。`build` が本体で、他の 2 つは補助。

## 4. 入力（書体）

3 系統。いずれか 1 つを指定する。

| 指定 | 意味 |
| --- | --- |
| `--google <family>` | キュレーション済み書体を**名前だけ**で取得（§4.1） |
| `--ttf <path\|url>` | 任意の TTF / OTF / WOFF / WOFF2 |
| `--font <name>` | 同梱ビットマップフォント（186 本） |

`--google` と `--ttf` はラスタライザを使うので `--em` が要る（§4.3）。
`--font` は既にビットマップなので不要。

### 4.1 `--google`

再配布可能（SIL OFL 1.1 / Apache-2.0）と確認済みの **31 書体**から名前で選ぶ。
グリフをファームウェアに焼くことは書体の再配布にあたるため、既定ではこの範囲に絞る。

```
Roboto / Roboto Mono / Roboto Condensed / Inter / Noto Sans / JetBrains Mono /
Oswald / Montserrat / Orbitron / Share Tech Mono / VT323 / Silkscreen / Micro 5 /
Tiny5 / Pixelify Sans / Noto Sans JP / Noto Serif JP / M PLUS 1 / M PLUS 1 Code /
M PLUS 2 / Kosugi Maru / Sawarabi Gothic / Zen Maru Gothic / BIZ UDGothic /
BIZ UDPGothic / DotGothic16 / Noto Sans Symbols 2 / Noto Sans SC / Noto Sans TC /
Noto Sans KR
```

**この一覧は近道であって制限ではない。** 一覧に無い書体は自分で `.ttf` を入手して
`--ttf` に渡せばよい。その場合ライセンスの確認は利用者の責任になる。

取得は Google Fonts の CSS API 経由。**書体まるごと 1 本の TTF を取ってキャッシュする**
（Noto Sans JP で 5.1 MB）。CSS API は User-Agent で返す形式を変えるため、
TTF を返す User-Agent を使う。

> 参考: 現代のブラウザの User-Agent では woff2 が `unicode-range` で 124 分割されて返る。
> 必要なスライスだけ取れば桁違いに軽いが、字ごとのスライス解決を自前で持つことになり、
> **「書体に無い字」と「スライスを取り忘れた字」の区別がつかなくなる**危険がある。
> v1 は 1 本まるごとを採る。後から最適化として差し替えても外から見た挙動は変わらない。

### 4.2 キャッシュ

取得したフォントは `node_modules/.cache/lgfx-font-tool/` に置く（`--cache-dir` で変更可）。
`--offline` でキャッシュのみを使い、無ければエラーにする。

### 4.3 サイズ — `--em`

**`--em N` は em（デザインサイズ）を画素で指定する。**

em は書体の設計上の 1 文字ぶんの正方形で、**全角 1 文字の送り幅がちょうど em になる。**
高さではなく**横**の寸法が定義であることに注意。

```
--em 16  →  全角 1 文字の送り幅が 16 画素
```

実測（Noto Sans JP）。`--em` と全角の送り幅は奇数でも常に一致する。

| `--em` | 行の高さ | 最大の墨面 | 全角の送り幅 | ラテンの送り幅 |
| --- | --- | --- | --- | --- |
| 8 | 10 | 10 | **8** | 6 |
| 10 | 12 | 12 | **10** | 7 |
| 12 | 14 | 14 | **12** | 9 |
| 14 | 16 | 16 | **14** | 10 |
| 16 | 19 | 18 | **16** | 12 |
| 20 | 23 | 22 | **20** | 15 |
| 24 | 29 | 28 | **24** | 17 |

**行の高さは em より大きくなる。** 上下の余白と、`|` や括弧のように em を超えて伸びる字が
あるため。**「16 画素の行に収めたい」なら `--em 16` ではなく `--em 14` を選ぶ**
（この対応は書体ごとに違うので、生成時に実測値を表示する）。

ラテン専従の書体には全角の字が無いので送り幅の一致は起きないが、em は同じく尺度として働く。

| `--em`（DejaVu Sans） | 行の高さ | 最大の墨面 | `H` の送り幅 |
| --- | --- | --- | --- |
| 12 | 14 | 13 | 9 |
| 16 | 17 | 16 | 12 |
| 24 | 26 | 25 | 18 |

### なぜ「基準字の高さ」ではないのか

**em は測定を伴わないので、文字集合を変えてもスケールが動かない。** これが決め手。

以前の実装は「優先リスト `[漢 国 日 가 H E N 0]` のうち**要求した集合に入っている**字の
墨面の高さ」を尺度にしていた。この方式には 2 つの問題があった。

- **文字を 1 字足すと既存の字が縮む。** ASCII 95 字に `日` を足すと基準字が `H` から `日` に
  変わり、**既存 92 字の寸法と行の高さが変わった**（実測）。
  「文字が気軽に増減できる」という前提を壊す
- **指定値が何も保証しない。** `16` と指定しても最大の墨面は 24 画素、行の高さは 25 画素に
  なりうる（基準字が最大の字とは限らないため）

`--em` は測定をしないのでどちらも起きない。ASCII 95 字に漢字 7 字を足しても
**変化した字は 0/95**（実測）。

`--em` は同梱ビットマップフォントの命名とも一致する。`lgfxJapanGothic_16` は
全角の送り幅が 16 で、`--em 16` と同じ意味になる。

### 収まりの検査

行の高さは書体依存なので、**入るかどうかは指定ではなく検査で扱う。**

```
--max-height 16    行の高さが 16 を超えたらエラー（終了コード 1）
```

## 5. 文字の指定

3 系統。**併用でき、和集合になる。**

| 指定 | 例 |
| --- | --- |
| `--chars <文字列>` | `--chars "温度設定完了℃"` |
| `--charset <path>` | `--charset fonts/chars.txt`（§10） |
| `--sets <id,...>` | `--sets ascii,hiragana,symUnits` |

### 集合 ID

| 軸 | ID（字数） |
| --- | --- |
| ラテン | `digits`(10) `ascii`(95) `latinExt`(96) `greek`(50) `cyrillic`(66) |
| かな | `hiragana`(91) `katakana`(95) `katakanaHalf`(63) `jaPunct`(31) |
| 漢字 | `hanJa1`(2139) `hanJa2`(3002) `hanJa3`(3295) `hanJa4`(6463) `hanCn1`(3755) `hanCn2`(6763) `hanTw1`(5411) `hanTw2`(13064) `hanKo1`(1799) `hanKo2`(4899) `hanAll`(20992) |
| ハングル | `hangulKs`(2350) `hangulAll`(11172) |
| 記号 | `symUnits`(44) `symMath`(41) `symArrows`(22) `symShapes`(56) `symCurrency`(20) `symEnclosed`(79) `symMisc`(35) |

### テンプレート

よくある組み合わせは `--template` で指定できる。

| ID | 字数 | ID | 字数 |
| --- | --- | --- | --- |
| `clock` | 10 | `japaneseUi` | 2,493 |
| `clockJa` | 41 | `japaneseFull` | 6,948 |
| `sensor` | 54 | `chineseUi` | 3,894 |
| `latinUi` | 233 | `koreanUi` | 2,489 |

## 6. 出力

`--out <path>` と `--format <id>`。**容器は拡張子で決まる。**

| 拡張子 | 容器 |
| --- | --- |
| `.h` / `.hpp` / `.c` / `.cpp` | C ソース（配列 + 構造体） |
| それ以外 | バイナリ / テキストをそのまま |

### 対応形式

| `--format` | C ソース | バイナリ | 備考 |
| --- | --- | --- | --- |
| `cellfont` | ○ | — | 既定。C ソースのみ（形式仕様 §12） |
| `u8g2` | ○ | ○ | `lgfx::U8g2font` として使える |
| `gfx` | ○ | ○ | Adafruit GFX 互換。疎な集合は LovyanGFX 拡張 |
| `vlw` | ○ | ○ | 実行時 `loadFont` |
| `bff` | ○ | ○ | 実行時 `loadFont`。`--bpp 1\|2\|4` |
| `bdf` | — | ○ | テキスト。フォントエディタ用 |
| `fontx2` | — | ○ | FatFs 界隈 |

形式と容器の組み合わせが不正なら、**理由を書いて終了する**
（例: `cellfont` は C ソースのみ、`bdf` はバイナリのみ）。

### 生成される C ソース

書体名・作者・ライセンス・入手元・収録範囲・データ量が、先頭のコメントに入る
（OFL / Apache は告知の同梱を求めるため）。**収録範囲がコメントに入るので、
`git diff` だけで「どの文字が増減したか」が読める。**

## 7. 検証モード

```
lgfx-font build ... --check
```

**出力ファイルを書かず、既存のものと一致するかだけを見る。** CI 用。
不一致なら差分の要点を出して終了コード 2。プレビュー（§8）は `--check` でも書ける
（`.h` の検証と画像出力は直交する）。

## 8. 確認用の画像

**画像は生成物ではなく生成物の確認**。凝らない。

| 指定 | 出力 |
| --- | --- |
| `--preview <path.png>` | グリフ一覧（頼んだ文字が入ったか） |
| `--preview <path.png> --preview-text "文字列"` | 文字列見本（実際の文言が読めるか） |

出力先はプロジェクトが決める。**コミットするか、`.gitignore` に入れるか、
CI のアーティファクトに上げるかはツールの関知するところではない。**

PNG は 8bit グレースケール。依存は `node:zlib` のみ。

## 9. 終了コード

| コード | 意味 |
| --- | --- |
| 0 | 成功 |
| 1 | 生成エラー（収録漏れ、`--max-height` 超過、フィールド範囲外、形式と容器の不整合など） |
| 2 | `--check` の不一致 |
| 3 | 引数の誤り |

**書体に無い文字は既定でエラー（コード 1）。** 黙って空白を焼き込むと実機で気づくまで
分からないため。`--allow-missing` で警告に落とせる。

## 10. 文字集合ファイル

`--charset` に渡すテキスト。`src/charsets/charsets.js` と同じ語彙を使う
（Web UI と同じ言葉になる）。

```
# 画面の固定文言
@ascii
@symUnits
U+00B0-U+00B1
温度湿度設定完了異常
```

| 行 | 意味 |
| --- | --- |
| `# ...` | コメント |
| `@<id>` | 集合 ID（§5） |
| `U+XXXX` / `U+XXXX-U+YYYY` | コードポイントと範囲 |
| その他 | その行に現れる文字すべて |

### 正準化

```
lgfx-font charset fonts/chars.txt --normalize
```

**コメントと構造は残し、リテラル文字だけを重複除去してコードポイント順に並べ替える。**
`@ascii` を 95 文字に展開はしない（**なぜその文字が要るのかが消えるため**）。

## 11. 使用例

```sh
# 単発。内蔵フォントから今欲しい文字だけ
npx lgfx-font build --font lgfxJapanGothic_16 --chars "温度設定完了" --out font.h

# Google Fonts から名前だけで
npx lgfx-font build --google "Noto Sans JP" --em 16 \
    --sets ascii,hiragana --chars "温度℃" --out font.h

# 手持ちの TTF から、確認画像つき
npx lgfx-font build --ttf ./MyFont.ttf --em 12 --charset chars.txt \
    --out font.h --preview font.png

# 形式を変えて
npx lgfx-font build --ttf ./MyFont.ttf --em 12 --sets ascii --format u8g2 --out font.u8g2

# 継続利用（package.json）
"scripts": {
  "font": "lgfx-font build --google 'Noto Sans JP' --em 16 --charset fonts/chars.txt --out src/font.h --preview fonts/preview.png"
}
```

日常はこうなる。

```
$ vim fonts/chars.txt
$ npm run font
  src/font.h   1,473 B (+29)   文字 191 (+1)   行高 16   最大墨面 16
               固定 4x8×95 + 固定 8x9×96   連鎖 2
$ git add -A && git commit
```

**`(+29)` と `(+1)` が要点。** 何を足したらいくら増えたかが毎回わかる。
既存の出力ファイルがあればそこから差分を出す。

**行高と最大墨面を毎回出す。** `--em` は横の寸法なので、縦が何画素になるかは
書体を替えるまで分からない。表示していれば `--max-height` を付けていなくても気づける。

## 12. 依存とネットワーク

- **ライブラリ本体は実行時依存ゼロのまま**（spec §3 判断 #2）
- **CLI パッケージはラスタライザに依存する。** TTF が主入力なので任意依存にしない
- ネットワークに触るのは `--google` と `--ttf <url>` のときだけ。
  `--offline` でキャッシュのみに制限できる

## 13. まだ決めていないこと

- `--format` の既定を `cellfont` にしてよいか（汎用ツールとして偏らないか）
- `--font`（同梱フォント）を `build` の入力に残すか。`inspect` だけにする案もある
- 複数ターゲットを 1 コマンドで出したくなったときの形（現状は npm script の `&&`）
- `--em` への変更はライブラリ（`src/gen/rasterize.js`）と Web UI にも入る。
  `measureTtf` / `pickProbe` / `PROBE_CANDIDATES` は不要になるので削除できる
